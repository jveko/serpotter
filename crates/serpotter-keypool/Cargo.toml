[package]
name = "serpotter-keypool"
version = "0.1.0"
edition.workspace = true

[dependencies]
serpotter-db = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["sync", "time"] }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "sync", "time"] }
sqlx = { workspace = true }
